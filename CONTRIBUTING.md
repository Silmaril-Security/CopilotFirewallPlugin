# Contributing

Run `npm ci`, `npm run typecheck`, and `npm test` before submitting a change. Keep hook failures fail-open, keep local evidence raw-content free, and update `plugin.json`, `package.json`, runtime constants, tests, and committed build output together when changing the plugin version.

